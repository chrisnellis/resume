<!--Custom Resume Theme-->
<!--Based on: jsonresume-theme-engineering (https://github.com/skoenig/jsonresume-theme-engineering)-->
<!--Original Author: Sören König-->
<!--License: MIT-->

<!--Modifications:-->
<!--- Allowed for end date-only items to render -->


<div class="item-details">

    {{#if startDate}}
        {{#if endDate}}
            <div class="date">
                {{formatDate startDate}} — {{formatDate endDate}}{{#if startDate2}}{{#if endDate2}}, {{formatDate startDate2}} —
                {{formatDate endDate2}}{{else}}, {{formatDate startDate2}} — Present{{/if}}{{/if}}
            </div>
        {{else}}
            <div class="date">
                {{formatDate startDate}} — Present
            </div>
        {{/if}}
    {{else}}
        {{#if endDate}}
            <div class="date">
                {{formatDate endDate}}
            </div>
        {{/if}}
    {{/if}}
</div>
